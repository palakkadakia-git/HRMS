# ============================================================
# HRMS — Root Environment Variables
# Copy this file and fill in values for each service.
# Each service also has its own .env file.
# ============================================================

# PostgreSQL
POSTGRES_USER=hrms
POSTGRES_PASSWORD=hrmspassword
POSTGRES_DB=hrmsdb
